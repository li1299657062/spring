eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
      name: spring
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: 8761
      label: master
